{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "lsp-compile",
      "type": "shell",
      "command": "npx tsc --project ./tsconfig-purs-ffi.json && npx spago run --main VsCodeExtension.Build && npx tsc --project ./lsp/tsconfig.json"
    }
  ]
}
