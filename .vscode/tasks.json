{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "vscode-extension-build",
      "type": "shell",
      "command": "npx tsc --project ./tsconfig-purs-ffi.json && npx spago run --main VsCodeExtension.Build"
    }
  ]
}
