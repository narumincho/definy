{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "start-vscode-extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/deno-lib/vscodeExtensionDistribution/",
        "--extensionDevelopmentKind=web"
      ]
      // "preLaunchTask": "vscode-extension-build"
    }
  ]
}
