{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LspExtension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/distribution/definy-extension",
        "--extensionDevelopmentKind=web"
      ],
      "preLaunchTask": "lsp-compile"
    }
  ]
}
