[workspace]

members = [
  "definy-build",
  "definy-client",
  "definy-event",
  "definy-server",
  "definy-ui"
]
resolver = "3"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

 