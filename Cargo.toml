[workspace]

members = [
  "definy-client",
  "definy-event",
  "definy-server",
  "definy-ui"
]
default-members = ["definy-client"]
resolver = "3"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

 