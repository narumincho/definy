import * as React from "react";
import { css } from "@emotion/css";

const TopPage = () => {
  return (
    <div className={css({ color: "white", backgroundColor: "green" })}>
      トップページだよ!
    </div>
  );
};

export default TopPage;
